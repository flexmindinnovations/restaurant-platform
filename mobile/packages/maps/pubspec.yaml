name: maps
description: Map integration for delivery tracking and restaurant locations
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  geolocator: ^14.0.0
  google_maps_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0
